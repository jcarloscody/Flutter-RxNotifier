name: server_app
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: '>=2.15.1 <3.0.0'

dependencies:
  #path: ^1.8.0
  socket_io: ^1.0.1
  common:
    path: ../common

dev_dependencies:
  lints: ^1.0.0
